{
    "extends": "../tsconfig.json",
    // lint *all* ts files in the project, not just the ones we want to compile
    "include": [
        "src/**/*.ts",
        "test/**/*.ts",
        "test/**/*.js",
        "site/**/*.ts"
    ],
}